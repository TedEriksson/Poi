<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/RelativeLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:columnCount="2"
    android:orientation="horizontal"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Home" >

    <Button
        android:id="@+id/searchMapButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="@string/search_by_map" />

    <ImageView
        android:id="@+id/poiLogoMain"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/searchIDButton"
        android:layout_alignLeft="@+id/searchMapButton"
        android:layout_alignParentTop="true"
        android:adjustViewBounds="true"
        android:baselineAlignBottom="false"
        android:contentDescription="@string/poi_logo"
        android:cropToPadding="false"
        android:scaleType="fitStart"
        android:src="@drawable/poi_logo" />

    <Button
        android:id="@+id/searchIDButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/searchMapButton"
        android:layout_alignParentRight="true"
        android:text="@string/search_by_id" />

    <EditText
        android:id="@+id/idNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/searchIDButton"
        android:layout_alignBottom="@+id/searchIDButton"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/searchIDButton"
        android:ems="10"
        android:inputType="number" >

        <requestFocus />
    </EditText>

</RelativeLayout>